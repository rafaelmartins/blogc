runner: blogc

jobs:
  distckeck-gcc:
    environment:
      - TARGET=distcheck
      - CC=gcc
  valgrind-gcc:
    environment:
      - TARGET=valgrind
      - CC=gcc

script: curl -sSf https://yatr.rgm.io/run.sh | bash
